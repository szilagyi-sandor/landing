const requiredCheckedComment = require("./requiredCheckedComment.js");

// TODO: add warning for older checks
module.exports = {
  rules: {
    "required-checked-comment": requiredCheckedComment,
  },
};
